# Sprint Backlog: Sprint 10

This document records the selected sprint backlog and planning decisions for Sprint 10.

## Sprint Summary

Capacity: 15 story points.

Selected implementation units for Sprint 10: 12/15 story points.

Status: ready

## Planning Notes

- Capacity decision: Sprint 10 capacity remains fixed at 15 story points, matching the Sprint 7, Sprint 8, and Sprint 9 planning baseline.
- Selection decision: Sprint 10 commits `Item 28`, `Item 31`, `Item 34`, and `Item 32` for a total of 12 story points.
- Ordering decision: Sprint 10 resequences Item 34 (coverage measurement, 2 SP) ahead of Item 32 (workflow_call, 5 SP) so the coverage enforcement gate is active before the largest surface expansion lands. The execution order is `Item 28` -> `Item 31` -> `Item 34` -> `Item 32`.
- Buffer decision: The remaining 3 story points are intentionally left unallocated. The buffer is larger than Sprints 7–9 (1–2 SP each) to account for Item 32's architectural novelty — the first discriminated union in the job model.
- Dependency decision: Item 32 depends on Items 28 (permissions shorthand) and 31 (identifier validation). Both are sequenced earlier. Item 34 has no dependencies and is infrastructure work. The execution order satisfies all prerequisites.
- Scope decision: Sprint 10 fills the top-priority SDK feature gap (workflow-level defaults and permissions shorthand), hardens identifier validation across all surfaces, adds coverage measurement and CI enforcement, and introduces the largest remaining feature — reusable workflow support.
- Item 28 decision: Workflow-level `defaults.run` reuses the existing `DefaultsRun` validation pattern from job-level defaults. Permissions shorthand (`read-all`, `write-all`) is accepted as an alternative to object maps at both workflow and job levels. Combining shorthand and object-map forms on the same scope fails at build time. Canonical top-level field order updated to: `name`, `on`, `permissions`, `defaults`, `env`, `concurrency`, `jobs`. Workflow-level `defaults.run` field order reuses existing: `shell`, `working-directory`. Canonical field order must be documented in SPEC.md before implementation, per Sprint 9 retrospective guidance.
- Item 31 decision: Shared identifier format validator uses `^[a-zA-Z_][a-zA-Z0-9_-]*$` matching GitHub Actions documentation. Applied uniformly to step IDs, matrix axis keys, and workflow dispatch input names. Validation error messages include the expected format pattern. Tests cover digit-leading IDs, special characters, spaces, and unicode. Existing valid identifiers must pass without regression.
- Item 34 decision: Coverage measurement uses Vitest v8 provider. Coverage scope is `packages/sdk/src/`. Threshold set at 100% for lines matching the project goal. `bun run coverage` produces line, branch, and function metrics. CI fails on threshold regression. Coverage report generated in lcov format. SPEC.md quality requirements and CONTRIBUTING.md are updated.
- Item 32 decision: `workflow_call` trigger supports `inputs` (per-input: `description`, `required`, `default`, `type`), `outputs` (per-output: `description`, `value` — `value` is required), and `secrets` (per-secret: `description`, `required`). Caller-side job-level `uses` references a reusable workflow with optional `with` (input bindings) and `secrets` (explicit map or `inherit` shorthand). Mutual exclusion between inline `steps` and reusable-workflow `uses` fails at build time. Job model uses a discriminated type: `StepsJob` vs `ReusableWorkflowJob`. Input, output, and secret names validated against the identifier format from Item 31. Canonical trigger key order updated to include `workflow_call` after `workflow_dispatch` and before `schedule`. Canonical reusable-workflow job field order: `if`, `needs`, `continue-on-error`, `permissions`, `secrets`, `with`, `uses` — mirroring how `steps` is last in a steps-based job. `workflow_call` trigger field order: `inputs`, `outputs`, `secrets`. Per-input field order: `description`, `required`, `default`, `type`. Per-output field order: `description`, `value`. Per-secret field order: `description`, `required`. Canonical field orders must be documented in SPEC.md before implementation, per Sprint 9 retrospective guidance.
- Estimate decision: Item 32 holds at 5 SP. The discriminated union and mutual exclusion complexity were already identified during the GPT review intake. No re-estimation.
- Resequencing rationale: Item 34 was globally prioritized after Item 32 (feature breadth first). Within Sprint 10, the Product Owner approved resequencing to position 3 because the rationale for Item 34 — "validates the 100% coverage goal before more surfaces are added" — favors activating the gate before the largest surface expansion. This is an intra-sprint execution optimization, not a backlog priority change.
- Conformance fixture rule: Every SDK-surface item (Items 28, 31, 32) must add or update shared cross-runtime conformance fixtures in the same slice, per the Sprint 7 retrospective guidance.
- Sprint 9 retrospective carryforward: (a) document canonical field order in SPEC.md before implementation for each SDK-surface item, (b) run `bun run format` before closeout commits to prevent formatting failures, (c) test-delegation splitting applies only when the item's test surface creates 15+ minute blocking wait — not uniformly to all 5+ SP items.
- Product Owner decision: Aoi Sakamoto approved the Sprint 10 scope, resequencing of Item 34, and all acceptance criteria sharpening. Canonical field orders for new surfaces are pre-defined and will be documented in SPEC.md before implementation.
- Scrum Master decision: Sprint 10 is ready to start with all planning decisions resolved, acceptance criteria sharpened through refinement, canonical orderings pre-defined, and implementation risks identified. The 3 SP buffer is appropriate for the architectural novelty in Item 32.
- Developer decision: Existing builder-time validation, deterministic renderer ordering, explicit unsupported-shape guardrails, Bun-primary verification, Node compatibility checks, and Deno compatibility evidence remain mandatory where they apply inside each Sprint 10 item.
- Collaboration decision: Sprint execution will keep one backlog item active at a time while using team personas within each item, with Aoi Sakamoto owning scope and acceptance decisions, Ren Takahashi owning sequencing, impediment tracking, and review readiness, Mio Kanda expected to lead SDK and architecture implementation, Haru Nishimura expected to lead validation and non-implementing review, and Yui Morita expected to support tooling and documentation follow-through within the active item.
- Risk assessment: Item 32 is the largest item and is last in execution order. If it runs long, the sprint delivers Items 28, 31, and 34 (7 SP) and Item 32 carries over. The 3 SP buffer absorbs unforeseen friction in earlier items without impacting Item 32's execution window.

## Committed Items

### Item 28: Workflow-level defaults and permissions shorthand

- Why: Workflow-level `defaults.run` (shared shell and working-directory for all jobs) and permissions shorthand forms (`read-all`, `write-all`) are commonly used in real workflows. The SDK currently supports `defaults.run` only at the job level and explicitly rejects permissions shorthand.
- Prerequisites: None directly. The existing job-level `defaults.run` and permissions object-map patterns provide clear extension models.
- Implementation Plan: Add workflow-level `defaults.run` to the workflow builder and AST, reuse the existing `DefaultsRun` validation, add shorthand permissions support (`read-all`, `write-all`) at both workflow and job levels as alternatives to object maps, validate mutual exclusion between shorthand and object-map forms, render in canonical positions, and update conformance fixtures.
- Definition of Done: Workflow-level `defaults.run` renders correctly, permissions shorthand forms are accepted alongside existing object maps, mutual exclusion is validated, rendering is deterministic, conformance fixtures cover both expansions across runtimes, SPEC.md is updated with canonical top-level field order including `defaults`, and the change is code reviewed by a non-implementing persona.
- Acceptance Criteria: Users can set `defaults.run` at the workflow level. Users can use `read-all` or `write-all` as permissions values at both workflow and job levels. Combining shorthand and object-map permissions on the same scope fails at build time. The rendered YAML matches GitHub Actions expected structure. Canonical top-level field order is `name`, `on`, `permissions`, `defaults`, `env`, `concurrency`, `jobs`. `defaults.run` field order is `shell`, `working-directory`. Cross-runtime conformance fixtures are added.
- Story Points: 3
- Status: done
- Completed At: 2026-04-01T12:41:56Z
- Notes/Links: [SPEC.md](../SPEC.md), [PR #27](https://github.com/moriturus/ghawb.ts/pull/27). This item widened the permissions boundary documented in SPEC.md to support shorthand forms and added workflow-level `defaults.run`. Closeout evidence: commits [`60f9db4`](https://github.com/moriturus/ghawb.ts/commit/60f9db44ab3e6d0738d909165827817f91c50f9a) and [`b7af997`](https://github.com/moriturus/ghawb.ts/commit/b7af997d1a857129a63b174470ca0f41993fc245), PR [`#27`](https://github.com/moriturus/ghawb.ts/pull/27) merged at `2026-04-01T12:41:56Z`, non-implementing review record by Haru Nishimura at [issue comment](https://github.com/moriturus/ghawb.ts/pull/27#issuecomment-4169826121), Product Owner acceptance by Aoi Sakamoto at [issue comment](https://github.com/moriturus/ghawb.ts/pull/27#issuecomment-4169827099), and verification via `bun run check`.

### Item 31: Identifier format validation hardening

- Why: Step IDs, matrix axis keys, and workflow dispatch input names are validated for non-blank and structural correctness but not against the GitHub Actions identifier format. Adding format validation catches malformed identifiers at SDK build time rather than deferring to GitHub Actions runtime failure.
- Prerequisites: Follows Sprint 9 (Items 24–27) so that matrix axis keys and dispatch input names are already implemented. ✅
- Implementation Plan: Define a shared identifier format validator using `^[a-zA-Z_][a-zA-Z0-9_-]*$`, apply it to step IDs, matrix axis keys, workflow dispatch input names, and any other identifier-like fields, update validation error messages to include the expected format, and add test coverage for format-invalid identifiers across all affected surfaces.
- Definition of Done: All identifier-like fields are validated against the GitHub Actions identifier format at build time, validation error messages clearly state the expected format, test coverage includes format-invalid cases for every affected surface, and the change is code reviewed by a non-implementing persona.
- Acceptance Criteria: Step IDs starting with a digit or containing invalid characters fail at build time with a clear format error including the expected pattern `^[a-zA-Z_][a-zA-Z0-9_-]*$`. Matrix axis keys with invalid characters fail at build time. Workflow dispatch input names with invalid characters fail at build time. The validator is shared across all identifier surfaces using the existing identifier factory pattern from `@ghawb/shared`. Existing valid identifiers continue to pass without regression. Tests cover digit-leading IDs, special characters, spaces, and unicode.
- Story Points: 2
- Status: done
- Completed At: 2026-04-01T12:52:28Z
- Notes/Links: [SPEC.md](../SPEC.md), [sprint_reviews/sp8.md](./sprint_reviews/sp8.md), [sprint_retrospectives/sp8.md](./sprint_retrospectives/sp8.md), [PR #28](https://github.com/moriturus/ghawb.ts/pull/28). Closeout evidence: commit [`56ee4f0`](https://github.com/moriturus/ghawb.ts/commit/56ee4f0f34b44e33f9c9f4af52a5e01b46a2dfb0), PR [`#28`](https://github.com/moriturus/ghawb.ts/pull/28) merged at `2026-04-01T12:52:28Z`, non-implementing review record by Haru Nishimura at [issue comment](https://github.com/moriturus/ghawb.ts/pull/28#issuecomment-4169885584), Product Owner acceptance by Aoi Sakamoto at [issue comment](https://github.com/moriturus/ghawb.ts/pull/28#issuecomment-4169886457), and verification via `bun run check`.

### Item 34: Coverage measurement and CI gate

- Why: The project goal is 100% code coverage, but coverage is currently stated as a goal without measurement or enforcement. Adding coverage measurement validates the quality claim, and adding a CI gate prevents coverage regression.
- Prerequisites: None. This is infrastructure work independent of feature delivery.
- Implementation Plan: Configure Vitest coverage reporting (v8 provider), add a `coverage` script to `package.json`, add coverage threshold enforcement to CI, generate coverage reports in CI artifacts (lcov format), and document the coverage contract in SPEC.md and CONTRIBUTING.md.
- Definition of Done: `bun run coverage` produces a coverage report for `packages/sdk/src/`. CI enforces coverage thresholds and fails on regression. Coverage report is available as a CI artifact. SPEC.md quality requirements and CONTRIBUTING.md document the coverage requirement. The change is code reviewed by a non-implementing persona.
- Acceptance Criteria: Running `bun run coverage` produces line, branch, and function coverage metrics via Vitest v8 provider. CI fails if coverage drops below the configured threshold. The coverage threshold is set at 100% for lines on `packages/sdk/src/`. Coverage reports are generated in lcov format for tooling integration. SPEC.md quality requirements section references the measurement mechanism. CONTRIBUTING.md documents how to run coverage locally.
- Story Points: 2
- Status: done
- Completed At: 2026-04-01T13:01:48Z
- Notes/Links: [SPEC.md](../SPEC.md), [package.json](../../package.json), [PR #29](https://github.com/moriturus/ghawb.ts/pull/29). Closeout evidence: commit [`f52c960`](https://github.com/moriturus/ghawb.ts/commit/f52c960f4f9b72ca9bad701fe1e1677e60c51372), PR [`#29`](https://github.com/moriturus/ghawb.ts/pull/29) merged at `2026-04-01T13:01:48Z`, non-implementing review record by Haru Nishimura at [issue comment](https://github.com/moriturus/ghawb.ts/pull/29#issuecomment-4169940132), Product Owner acceptance by Aoi Sakamoto at [issue comment](https://github.com/moriturus/ghawb.ts/pull/29#issuecomment-4169941121), and verification via `bun run coverage` plus `bun run check`. Implementation note: the delivered Vitest v8 coverage path required adding `@vitest/coverage-v8` as a dev dependency in this repository.

### Item 32: Reusable workflow support — workflow_call trigger with inputs, outputs, and secrets

- Why: Reusable workflows (`workflow_call`) are one of the most impactful GitHub Actions features for reducing duplication. The external GPT review identified broader syntax coverage as the top priority. `workflow_call` is the single highest-value missing trigger type because it enables an entirely new workflow composition pattern.
- Prerequisites: Item 28 (permissions shorthand) and Item 31 (identifier validation). Both are sequenced earlier in this sprint.
- Implementation Plan: Add `workflow_call` as a new trigger type with `inputs` (per-input `description`, `required`, `default`, `type`), `outputs` (per-output `description`, `value` — `value` is required), and `secrets` (per-secret `description`, `required`). On the caller side, extend the job model with a discriminated union: `StepsJob` vs `ReusableWorkflowJob`. A reusable-workflow job uses `uses` (referencing `owner/repo/.github/workflows/file.yml@ref`) with optional `with` (input bindings) and `secrets` (explicit map or `inherit` shorthand). Validate mutual exclusion between steps-based jobs and reusable-workflow jobs. Apply identifier format validation from Item 31 to input, output, and secret names. Render in canonical positions. Update conformance fixtures. Update SPEC.md.
- Definition of Done: `workflow_call` trigger renders with inputs, outputs, and secrets. Caller jobs using reusable workflows render with `uses`, `with`, and `secrets`. Mutual exclusion between steps-based and reusable-workflow jobs is validated. Discriminated union type is used in the job model. Identifier format validation is applied to input, output, and secret names. Conformance fixtures cover the new trigger and caller patterns across runtimes. SPEC.md is updated with canonical orderings. The change is code reviewed by a non-implementing persona.
- Acceptance Criteria: Users can define `workflow_call` triggers with typed inputs, outputs, and secrets. Users can create jobs that call reusable workflows via `uses` with input bindings and secret bindings. A job cannot have both inline steps and a reusable workflow `uses` reference — fails at build time. `secrets: inherit` is accepted as a shorthand. Input, output, and secret names are validated against the identifier format. Canonical trigger key order: `push`, `pull_request`, `workflow_dispatch`, `workflow_call`, `schedule`. Canonical `workflow_call` trigger field order: `inputs`, `outputs`, `secrets`. Per-input: `description`, `required`, `default`, `type`. Per-output: `description`, `value`. Per-secret: `description`, `required`. Canonical reusable-workflow job field order: `if`, `needs`, `continue-on-error`, `permissions`, `secrets`, `with`, `uses`. Cross-runtime conformance fixtures are added.
- Story Points: 5
- Status: done
- Completed At: 2026-04-01T13:25:43Z
- Notes/Links: [SPEC.md](../SPEC.md), [PR #30](https://github.com/moriturus/ghawb.ts/pull/30). Closeout evidence: commit [`36cf0bc`](https://github.com/moriturus/ghawb.ts/commit/36cf0bc6f4dbc0a337d8c55d73340fd15069a20a), PR [`#30`](https://github.com/moriturus/ghawb.ts/pull/30) merged at `2026-04-01T13:25:43Z`, non-implementing review record by Haru Nishimura at [issue comment](https://github.com/moriturus/ghawb.ts/pull/30#issuecomment-4170088089), Product Owner acceptance by Aoi Sakamoto at [issue comment](https://github.com/moriturus/ghawb.ts/pull/30#issuecomment-4170088107), and verification via `bun run check` plus `bun run coverage`. This is a major surface expansion introducing a new job variant (reusable-workflow job vs. steps-based job). ADR 0001 explicit-boundary pattern was preserved. The `workflow_call` input type set reuses `workflow_dispatch` types but rejects `choice` and `options` per the sprint decision.
