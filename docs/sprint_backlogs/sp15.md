# Sprint Backlog: Sprint 15

This document records the selected sprint backlog and planning decisions for Sprint 15.

## Sprint Summary

Capacity: 20 story points.

Selected implementation units for Sprint 15: 6/20 story points.

Status: ready — 3 items committed (6 SP).

## Planning Notes

- Capacity decision: Sprint 15 uses a 20 SP capacity (matching Sprint 14). The selected load is 6 SP with an intentional 14 SP buffer. The buffer reflects genuine backlog exhaustion after 14 sprints and 49 delivered items — no forced intake is needed.
- Selection decision: Sprint 15 commits `Item 49`, `Item 50`, and `Item 48` for a total of 6 SP.
- Ordering decision: Sprint 15 follows the committed order `Item 49` → `Item 50` → `Item 48`. The blocking npm bug fix lands first, local verification tooling gap closure second, and the SDK feature enhancement last. This follows the principle: fix the foundation before building new surface.
- Priority decision: The Product Owner (Aoi Sakamoto) decided during the Sprint 14 review that Item 49 must be prioritized ahead of Item 48 because `npm install` failure blocks npm consumers. The product backlog file listed Item 48 first, but the PO's explicit priority decision governs.
- Item 50 intake decision: Item 50 was created during Sprint 15 planning to formally close the Scrum Master BOARD item from the Sprint 14 retrospective. The tsc build-config gap was identified as the root cause of the Sprint 14 CI failure — `bun run check` includes `tsc -p tsconfig.json --noEmit` (root typecheck) but does not include the per-package `tsc -p packages/*/tsconfig.build.json` build compilation that CI runs. The team agreed to track this as a formal backlog item (1 SP) following the project convention where process improvements involving code changes are tracked as backlog items (e.g., Item 30, Item 34).
- Item 49 refinement decision: Item 49's acceptance criteria were refined during planning to include a CI verification step that runs `npm install` to prevent regression, per Yui Morita's Sprint 14 retrospective recommendation.
- Dependency decision: All three items are independent with no blocking dependencies. Item 49, Item 50, and Item 48 can execute in any order, but the committed order follows the PO's priority decision and the principle of fixing tooling before adding features.
- Collaboration decision: Sprint execution remains single-item-at-a-time in committed order per working agreements. Role-based collaboration follows `TEAM.md`. Product Owner support focuses on Item 49 scope (npm compatibility strategy) and Item 48 acceptance (reusable workflow ergonomics). Scrum Master support focuses on Item 50 BOARD closure and sequencing. Developer work should be smallest-reviewable within each item.

## Committed Items

### Item 49: Fix npm install/ci failure caused by workspace: protocol

- Why: `npm install` and `npm ci` fail with `EUNSUPPORTEDPROTOCOL` because `package.json` files use `workspace:*` for inter-package dependencies. The `workspace:` protocol is supported by pnpm, yarn, and bun, but not by npm. Since the project aims to maintain package manager compatibility across npm, yarn, and pnpm (per AGENTS.md), npm must work.
- Prerequisites: None.
- Implementation Plan:
  - Investigate viable workspace dependency strategies that work across npm, pnpm, yarn, and bun simultaneously.
  - Likely approach: replace `workspace:*` with `file:../shared` or similar npm-compatible references, or adopt npm workspaces configuration in root `package.json`.
  - Verify that the chosen strategy does not break `npm publish` — published packages must reference registry versions, not local paths.
  - Verify `bun install`, `pnpm install`, `yarn install`, and `npm install` all succeed.
  - Verify `npm publish --dry-run` still produces correct dependency references for each package.
  - Add an `npm install` verification step to CI to prevent regression.
  - Update RELEASING.md if the publish flow is affected.
- Definition of Done:
  - `npm install` and `npm ci` succeed without errors.
  - `bun install`, `pnpm install`, and `yarn install` continue to work.
  - Published packages reference correct registry versions (not local paths).
  - CI includes an `npm install` verification step to prevent regression.
  - All tests pass after the change.
  - Code review completed.
- Acceptance Criteria:
  - `npm install` exits 0 in a clean checkout.
  - `npm ci` exits 0 with a valid lockfile.
  - `npm publish --dry-run` for each package shows correct dependency versions.
  - CI includes an `npm install` step that prevents future `workspace:` protocol regressions.
- Story Points: 2
- Status: done
- Completed At: 2026-04-03
- Notes/Links: Root cause was `workspace:*` in `package.json` (root), `packages/cli/package.json`, and `packages/sdk/package.json`. Fixed by replacing with `^0.1.0` semver ranges. `package-lock.json` committed for `npm ci` support. CI `npm install --dry-run` guardrail added per Yui Morita's Sprint 14 retrospective recommendation. PR #52.

### Item 50: Close tsc build-config gap in local verification

- Why: The Sprint 14 CI failure was caught by `tsc -p packages/*/tsconfig.build.json` (per-package build compilation), which is not included in `bun run check`. The root `typecheck` script runs `tsc -p tsconfig.json --noEmit` but does not cover the per-package build configs that CI runs separately. This creates a gap where Bun-accepted type mismatches pass locally but fail in CI.
- Prerequisites: None.
- Implementation Plan:
  - Add per-package `tsc` build compilation to the `typecheck` script or a new `build:check` script, and wire it into `bun run check`.
  - Verify that `bun run check` catches the class of error that caused the Sprint 14 CI failure (e.g., passing `string` where `readonly string[]` is expected in a constructor call).
  - Close the Scrum Master BOARD item from the Sprint 14 retrospective.
- Definition of Done:
  - `bun run check` fails locally for the same type errors that CI's build step catches.
  - Per-package `tsconfig.build.json` compilation is included in the local verification loop.
  - The Scrum Master BOARD item is moved to Closed in `docs/scrum_master/BOARD.md`.
  - Code review completed.
- Acceptance Criteria:
  - Running `bun run check` with a simulated type mismatch (e.g., passing `string` where `readonly string[]` is expected) fails locally before reaching CI.
  - Per-package `tsconfig.build.json` compilation is included in `bun run check` or a script that `bun run check` invokes.
- Story Points: 1
- Status: done
- Completed At: 2026-04-03
- Notes/Links: Added `build:check` script to `bun run check`. Closed Scrum Master BOARD item from Sprint 14 retrospective. See [LEARN.md](../LEARN.md) entry "Bun runtime accepts mismatched constructor argument types that strict tsc rejects." PR #53.

### Item 48: Reusable workflow object injection in usesWorkflow()

- Why: Currently `usesWorkflow()` only accepts a `WorkflowRef` string. Allowing a `WorkflowBuilder` (unbuilt) or `WorkflowDefinition` (built) to be passed directly improves ergonomics and enables safety — the SDK can verify the target workflow declares a `workflow_call` trigger at the caller's `build()` time without imposing definition-order constraints.
- Prerequisites: None (reusable workflow support is already complete via Item 32).
- Implementation Plan:
  - Define a `ReusableWorkflowSource` union type: `WorkflowRef | WorkflowBuilder | WorkflowDefinition`.
  - Extend `usesWorkflow()` signature to accept `ReusableWorkflowSource`.
  - When a `WorkflowBuilder` or `WorkflowDefinition` is provided, derive the local ref as `./.github/workflows/${id}.yml`.
  - Store a reference to the builder/definition alongside the derived ref so `build()` can inspect triggers later.
  - At the caller's `build()` time (not at `usesWorkflow()` call time), validate that the target workflow has a `workflow_call` trigger. This avoids definition-order constraints — `onWorkflowCall()` may be called after `usesWorkflow()`.
  - Add builder tests for object injection (happy path with builder, happy path with definition, missing trigger error, with/secrets forwarding, definition-order independence).
  - Add conformance fixture for object-injected reusable workflow rendering.
  - Update SPEC.md with the new overload.
- Definition of Done:
  - `usesWorkflow()` accepts `WorkflowRef`, `WorkflowBuilder`, and `WorkflowDefinition`.
  - Passing a workflow without `workflow_call` trigger produces a validation error at the caller's `build()` time.
  - Rendered output is identical to string-ref usage (`./.github/workflows/{id}.yml`).
  - Definition order is unconstrained — `usesWorkflow(builder)` works even if `builder.onWorkflowCall()` is called later.
  - 100% line coverage maintained.
  - SPEC.md updated.
  - Code review completed.
- Acceptance Criteria:
  - `job.usesWorkflow(unbuiltBuilder, { secrets: "inherit" })` compiles and renders correct `uses:` value.
  - `job.usesWorkflow(builtDefinition, { secrets: "inherit" })` also works.
  - Passing a workflow without `workflow_call` trigger to `usesWorkflow()` is accepted, but `build()` produces a clear validation error.
  - All existing `usesWorkflow(stringRef)` usage continues to work unchanged.
- Story Points: 3
- Status: ready
- Completed At: N/A
- Notes/Links: Requested during Sprint 14 execution. Enhances the reusable workflow API surface (Item 32). Validation deferred to caller `build()` time per design decision.
