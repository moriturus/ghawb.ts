# Sprint Backlog: Sprint 16

This document records the selected sprint backlog and planning decisions for Sprint 16.

## Sprint Summary

Capacity: 20 story points.

Selected implementation units for Sprint 16: 19/20 story points.

Status: ready — 5 items committed (19 SP).

## Planning Notes

- Capacity decision: Sprint 16 uses a 20 SP capacity (matching Sprints 14–15). The selected load is 19 SP with 1 SP residual.
- Selection decision: Sprint 16 commits all five remaining product backlog items — `Item 51`, `Item 52`, `Item 53`, `Item 54`, and `Item 55` — for a total of 19 SP. This exhausts the current product backlog.
- Ordering decision: Sprint 16 follows the PO-confirmed order `Item 51` → `Item 52` → `Item 53` → `Item 54` → `Item 55`. SDK-value items lead (expression helpers, runs-on expansion), followed by CLI-adjacent verification tooling, then adoption documentation, and finally the opt-in migration package. This follows the principle: strengthen the core type-safe authoring proposition first, expand syntax coverage second, improve verification third, invest in documentation fourth, and defer the interoperability package until core direction is proven.
- Estimate refinement decision: All five estimates were validated against implementation evidence during refinement. Discovery-inferred story points were confirmed at their original values: Item 51 (5 SP), Item 52 (3 SP), Item 53 (3 SP), Item 54 (3 SP), Item 55 (5 SP).
- Item 53 CLI-readiness decision: AGENTS.md prioritizes SDK/renderer work ahead of CLI work. The PO priority order is sufficient to satisfy this constraint — sequential execution ensures Items 51–52 (SDK work) land before Item 53 (CLI-adjacent work) begins.
- Item 55 spec prerequisite decision: The `docs/SPEC.md` update describing the new YAML-import package boundary is part of Item 55's Definition of Done rather than a prerequisite that must land before implementation starts. ADR 0001 already permits a separate opt-in package.
- Acceptance criteria refinement (Item 51): The MVP expression helper surface covers context reference helpers (`github`, `env`, `secrets`, `steps.<id>.outputs`, `matrix`, `inputs`), status-check function helpers (`success()`, `always()`, `cancelled()`, `failure()`), and expression string construction that wraps values in `${{ }}` automatically. All existing `string` entry points remain unchanged; helpers are additive. Build-time validation covers syntax-level checks (empty/blank rejection, valid `${{ }}` wrapper construction). Semantic evaluation (operator precedence, type coercion, runtime context availability) is an explicit non-goal for MVP.
- Acceptance criteria refinement (Item 52): All three object-form combinations are supported: `group`-only, `labels`-only, and `group`+`labels` together. At least one field must be present in the object form. Object form and string/array form are mutually exclusive on the same job.
- Acceptance criteria refinement (Item 53): The verification path is a new CLI command or flag. When `actionlint` is not found on `PATH`, the CLI exits with a non-zero code and a message naming the missing tool and linking to installation instructions — not a silent skip or a zero-exit fallback.
- Acceptance criteria refinement (Item 55): The import function accepts a file-system path to a YAML file (not raw YAML strings in MVP). The return type is an object that can be passed to `usesWorkflow()` through the existing `ReusableWorkflowSource` boundary. The package depends on a YAML parser internally but does not re-export it.
- Dependency decision: All five items are independent with no blocking dependencies. Sequential execution follows the PO-confirmed priority order.
- Collaboration decision: Sprint execution remains single-item-at-a-time in committed order per working agreements. Role-based collaboration follows `TEAM.md`. Primary implementation responsibility: Mio Kanda (SDK/Architecture) for Items 51, 52, 55; Yui Morita (Tooling/Workflow) for Items 53, 54. Haru Nishimura (Quality/Testing) provides cross-item review and validation coverage. Product Owner support focuses on MVP scope boundaries (Item 51) and package-boundary acceptance (Item 55). Scrum Master support focuses on sequential ordering enforcement and capacity tracking across 5 items.
- Sprint branch: `sprint-16` from `origin/main`.

## Committed Items

### Item 51: Expression helper MVP

- Why: The current SDK still relies on raw `${{ }}` strings for expressions. Sprint 15 discovery identified expression helpers as the clearest remaining type-safety gap in a product that positions type safety as a primary value.
- Prerequisites: None.
- Implementation Plan: Design and implement a minimal expression-helper API that covers the highest-value authoring paths first, such as common `if` conditions and expression-bearing values used in job or step metadata. The MVP helper surface covers context reference helpers (`github`, `env`, `secrets`, `steps.<id>.outputs`, `matrix`, `inputs`) and status-check function helpers (`success()`, `always()`, `cancelled()`, `failure()`). Helpers produce valid `${{ }}` strings automatically. Preserve the existing builder-centered API style, add build-time validation where practical (syntax-level, not semantic evaluation), update deterministic rendering and cross-runtime conformance fixtures, and document the supported helper surface in `docs/SPEC.md` and API-facing documentation.
- Definition of Done: A documented expression-helper MVP exists in the SDK, representative expression authoring paths no longer require raw string interpolation, runtime validation remains explicit, deterministic rendering is preserved, cross-runtime fixtures are updated, and code review is completed.
- Acceptance Criteria: SDK consumers can construct representative expression values through helpers instead of raw `${{ }}` strings for context references and status-check functions. Existing raw string entry paths remain backward compatible. Helpers produce syntactically valid `${{ }}` wrappers; empty or blank expressions are rejected at build time. Semantic evaluation (operator precedence, type coercion, runtime context availability) is an explicit non-goal. `docs/SPEC.md` documents the MVP scope and any intentional non-goals.
- Story Points: 5
- Status: done
- Completed At: 2026-04-03T15:30:00Z

### Item 52: Advanced `runs-on` support

- Why: The syntax coverage matrix still lists the `runs-on.group` / `runs-on.labels` object form as unsupported. This is a concrete remaining workflow-syntax gap with practical value for self-hosted and managed-runner scenarios.
- Prerequisites: None.
- Implementation Plan: Extend the step-based job model, builder API, validation, deterministic renderer, and conformance fixtures to support the GitHub Actions object form of `runs-on`, covering `group` and `labels` while preserving the current string and string-array entry paths. All three object-form combinations are supported: `group`-only, `labels`-only, and `group`+`labels` together. At least one field must be present in the object form. Object form and string/array form are mutually exclusive on the same job.
- Definition of Done: Step-based jobs support `runs-on` object authoring in addition to the existing forms, explicit validation covers supported and unsupported combinations, deterministic rendering preserves canonical field order, cross-runtime fixtures are updated, and code review is completed.
- Acceptance Criteria: SDK consumers can define `runs-on` with `group` and/or `labels` through a documented API. All three combinations (`group`-only, `labels`-only, `group`+`labels`) are supported. Object form and string/array form are mutually exclusive on the same job. Unsupported or malformed `runs-on` object shapes fail explicitly at build time. Existing string and string-array `runs-on` behavior remains unchanged. `docs/SPEC.md` and `docs/SYNTAX_COVERAGE.md` are updated.
- Story Points: 3
- Status: done
- Completed At: 2025-07-25T00:30:00Z

### Item 53: `actionlint` bridge for generated workflow verification

- Why: The README already positions `ghawb` and `actionlint` as complementary, but the current product stops at documentation. Discovery identified a gap between authoring-time safety and YAML-level static validation in the day-to-day workflow.
- Prerequisites: None. Assumption: `actionlint` remains an external optional tool rather than a core dependency.
- Implementation Plan: Add a CLI command or flag that can invoke `actionlint` against generated workflow files after rendering. When `actionlint` is not found on `PATH`, exit with a non-zero code and a message naming the missing tool and linking to installation instructions — not a silent skip or a zero-exit fallback. Surface clear failure diagnostics from `actionlint` output. Keep the core SDK and AST architecture independent from YAML parsing concerns.
- Definition of Done: Users have a documented supported path to run `actionlint` from the CLI tooling, failures are reported clearly, optional-tool absence is handled explicitly with a non-zero exit and actionable install message, architecture boundaries remain intact, and code review is completed.
- Acceptance Criteria: A supported CLI command or flag can verify generated workflow files with `actionlint`. When `actionlint` is unavailable, the CLI exits non-zero with a clear actionable message naming the tool and linking to installation instructions. The implementation does not add YAML input/parser support to the core SDK architecture. `README.md` and `docs/SPEC.md` are updated as needed.
- Story Points: 3
- Status: done
- Completed At: 2025-07-25T00:40:00Z

### Item 54: API reference and cookbook documentation

- Why: Sprint 15 retrospective explicitly identified user-facing documentation beyond the README as a candidate next theme. The repository also carries API-discovery gotchas, such as the job builder's `.displayName()` naming, that are better addressed through durable reference material.
- Prerequisites: None.
- Implementation Plan: Add durable external-facing documentation for the SDK, covering API reference material, a cookbook of representative workflow patterns, and explicit notes for known naming or ergonomics gotchas. Keep the documentation aligned with `docs/SPEC.md`, `README.md`, and the current implemented surface. Document the `displayName()` vs. `.name()` asymmetry explicitly as a known gotcha per the recommendation in `docs/LEARN.md`.
- Definition of Done: A documented API reference and cookbook exist, representative usage paths are covered, known API gotchas are called out (including `displayName()`), relevant index links are added, and code review is completed.
- Acceptance Criteria: A user can move from the README into deeper API-facing documentation for common workflow patterns and key builder APIs. The documentation explicitly covers at least one naming/ergonomics gotcha already recorded in repository lessons (specifically the `displayName()` naming decision). `docs/INDEX.md` is updated if new docs are added under `docs/`.
- Story Points: 3
- Status: done
- Completed At: 2025-07-25T00:50:00Z

### Item 55: Opt-in reusable-workflow import package

- Why: Discovery identified a real migration and interoperability need for users who already have reusable workflows authored as committed YAML files and want to reference them from the `ghawb` API surface. The current architecture supports reusable-workflow injection for `WorkflowBuilder`, `WorkflowDefinition`, and `WorkflowRef`, but intentionally keeps YAML input out of the core SDK. An opt-in package can address this use case without weakening the SDK's explicit AST and emitter boundaries.
- Prerequisites: None. Constraint: the implementation must keep YAML parsing and import logic out of `@ghawb/sdk` and treat the feature as optional migration or interoperability tooling rather than as a core authoring path.
- Implementation Plan: Design and implement a separate package, tentatively `@ghawb/reusable-workflow-import` or equivalent, that can read a committed reusable workflow YAML file from a file-system path, validate that it is a supported `workflow_call` target, and return an object that can be passed to `usesWorkflow()` through the existing `ReusableWorkflowSource` boundary. The package may depend on a YAML parser internally but must not re-export it, and `@ghawb/sdk` must remain YAML-input-free. Scope the first slice narrowly around reusable-workflow import rather than general workflow round-tripping. Document the architectural boundary, supported input scope, failure modes, and integration path. Update `docs/SPEC.md` as part of the Definition of Done.
- Definition of Done: An optional package exists that imports supported reusable-workflow YAML definitions from file-system paths through a documented API, validates `workflow_call` suitability, integrates with the main authoring flow through the `ReusableWorkflowSource` boundary, preserves the SDK's no-YAML-input contract, `docs/SPEC.md` is updated to document the package boundary, and the package is covered by tests and code review.
- Acceptance Criteria: Users can opt into a separate package to load a supported reusable workflow YAML file by file-system path and pass the imported result into the `ghawb` authoring flow via `usesWorkflow()` without adding a YAML dependency to `@ghawb/sdk`. Invalid YAML, unsupported workflow shapes, or non-reusable-workflow inputs fail with clear diagnostics. `docs/SPEC.md` and package-facing documentation describe the boundary explicitly: reusable-workflow import is optional tooling, not core SDK behavior.
- Story Points: 5
- Status: done
- Completed At: 2025-07-25
- Notes/Links: Discovery intake from the current [Specification](../SPEC.md), [ADR 0001](../adrs/0001-record-architecture-principles.md), [Sprint 15 Review](../sprint_reviews/sp15.md), and existing reusable-workflow object injection behavior. Estimate validated during Sprint 16 refinement against codebase evidence (3 existing packages, YAML parser already used in CLI, `ReusableWorkflowSource` union already modeled). Delivered via PR #63.
