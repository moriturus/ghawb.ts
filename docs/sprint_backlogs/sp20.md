# Sprint Backlog: Sprint 20

This document records the selected sprint backlog and planning decisions for Sprint 20.

## Sprint Summary

Capacity: 20 story points.

Selected implementation units for Sprint 20: 18/20 story points.

Status: in progress — 1/8 items completed (1/18 SP).

## Planning Notes

- Capacity decision: Sprint 20 keeps the standing 20 SP capacity and commits 18 SP, leaving 2 SP residual. The residual is intentional planning buffer for the newly introduced ad hoc package-manager repair and the discovery-first split of composite action work.
- Selection decision: Sprint 20 commits `Ad hoc A1`, Items 67, 68, 69, 70a, 70b, 71, and 72 for a total of 18 SP. Item 73 remains in the product backlog as the next deferred item.
- Ordering decision: Sprint 20 preserves strict backlog execution order: `Ad hoc A1` -> `Item 67` -> `Item 68` -> `Item 69` -> `Item 70a` -> `Item 70b` -> `Item 71` -> `Item 72`. The ad hoc item is inserted first because `npm ci` failure is an active repository readiness impediment.
- Deferral decision: Item 73 is deferred to preserve a small planning buffer and to avoid mixing a broader onboarding-doc expansion into a sprint that already contains CLI ergonomics, trigger completion, package-manager repair, typed-wrapper expansion, and composite-action discovery plus delivery.
- Item 68 scope decision: The inferred output-path contract is limited to `ghawb render`, not `render-batch`. Inference is constrained to the repository-local supported workflow-source convention and must fail explicitly for unsupported or ambiguous inputs.
- Item 69 scope decision: The sprint item must at least deliver `branch_protection_rule` and `deployment_protection_rule`. Any GitHub App event scope not completed inside the same slice must be narrowed explicitly in docs or split into a follow-up backlog item rather than left vague.
- Item 70 split decision: Original Item 70 was split into `Item 70a` (discovery spike) and `Item 70b` (initial implementation slice) because the package boundary, AST ownership, and output path for composite actions are not yet decision-complete. `Item 70b` does not begin until `Item 70a` is review-complete and has produced a concrete minimal slice.
- Item 70a discovery decision: The spike must compare at least the `@ghawb/sdk`-hosted path versus a separate package path, clarify the minimum composite-action AST surface, decide whether emission stays on an existing CLI path or requires a dedicated action emitter flow, and document explicit supported and unsupported scope for the first slice.
- Collaboration decision: Sprint execution remains single-item-at-a-time in committed order. Sub-agent or multi-agent collaboration is preferred only inside the active item when it materially improves throughput or separation of concerns. Primary implementation responsibility: Mio Kanda (`Ad hoc A1`, Items 67, 68, 69, 70a, 70b, 71, and 72). Verification and review focus: Haru Nishimura. Product-surface acceptance and scope judgment: Aoi Sakamoto. Tooling and documentation friction support: Yui Morita. Planned review ownership: Haru Nishimura reviews `Ad hoc A1`, Items 67, 68, 69, 70b, 71, and 72 for correctness and coverage; Aoi Sakamoto reviews `Item 70a` for product-direction fit.
- Documentation decision: Sprint 20 is expected to update `docs/PRODUCT_BACKLOG.md`, `docs/sprint_backlogs/sp20.md`, and likely `docs/SPEC.md`, `README.md`, `docs/COOKBOOK.md`, `docs/SYNTAX_COVERAGE.md`, and package-specific API documentation depending on delivered items. `docs/INDEX.md` was updated at planning time because `sp20.md` was added.
- Verification decision: The sprint continues to use `bun test` for targeted proof, `bun run check` as the primary integrated quality gate, `npm ci` as explicit package-manager compatibility proof for `Ad hoc A1`, and Node-run Vitest as the secondary runtime check. Deno remains smoke-level compatibility coverage rather than the primary sprint test runner.
- Sprint branch: `sprint-20` from `main`.

## Committed Items

### Ad hoc A1: Fix npm ci lockfile/workspace drift

- Why: `npm ci` currently fails because the lockfile is out of sync with the repository's workspace package set, breaking npm-based clean installs and weakening the repository's package-manager compatibility contract.
- Prerequisites: None.
- Implementation Plan: Update `package-lock.json` to match the current workspace configuration, including the missing `@ghawb/typed-actions` and `@ghawb/yaml-import` entries, and verify that `npm ci` succeeds from a clean install path without regressing the existing Bun/Node quality gates.
- Definition of Done: Root-level `npm ci` succeeds with the committed lockfile, the lockfile reflects the current workspace package set, and the item has code review completed by a non-implementing persona.
- Acceptance Criteria: Running `npm ci` at the repository root completes without the current sync error. The lockfile includes the workspace package metadata needed for `@ghawb/typed-actions@0.1.0` and `@ghawb/yaml-import@0.1.0`. Existing Bun- and Node-based verification commands remain intact.
- Story Points: 1
- Status: done
- Completed At: 2026-04-04T15:51:08Z
- Notes/Links: Planning-time reproduction on 2026-04-05 showed `npm ci` failing with `EUSAGE` because `package-lock.json` was missing `@ghawb/typed-actions@0.1.0` and `@ghawb/yaml-import@0.1.0`. Delivered by adding the missing workspace entries to `package-lock.json` so `npm ci` succeeds again. Verification: `npm ci`, `bun run check`, and `npm run test:vitest:node` all passed. PR #78.

### Item 67: CLI short flags for render input/output

- Why: `ghawb render` and `ghawb render-batch` currently require verbose `--input` / `--output` flags for every mapping. Short aliases `-i` and `-o` would reduce command noise and make repeated CLI usage less tedious, especially in batch mode and shell snippets.
- Prerequisites: None. Builds on the existing render and render-batch argument parser.
- Implementation Plan: Add `-i` as a shorthand for `--input` and `-o` as a shorthand for `--output` in both `render` and `render-batch`. Preserve existing long flags unchanged. Update CLI usage text, README examples where appropriate, and tests covering mixed long/short flag usage.
- Definition of Done: The CLI accepts `-i` / `-o` anywhere `--input` / `--output` are currently accepted, with unchanged semantics and validation behavior. Tests, docs, and code review completed.
- Acceptance Criteria: `ghawb render -i workflows/ci.ts -o .github/workflows/ci.yml` works. `ghawb render-batch` accepts repeated `-i` / `-o` pairs. Existing long-flag usage remains backward compatible.
- Story Points: 1
- Status: ready
- Completed At: N/A
- Notes/Links: User-requested CLI ergonomics improvement after Sprint 18.

### Item 68: CLI inferred default output path for render

- Why: Requiring `--output` for the common case creates friction when the desired destination is the conventional `.github/workflows/<name>.yml`. Inferring the output path from the input filename would make the CLI more convenient for routine repository-local rendering.
- Prerequisites: Item 67 is adjacent but not technically required. Depends on clarifying the supported inference contract before implementation.
- Implementation Plan: Design and implement a default-output rule for `ghawb render` that derives `.github/workflows/<input-basename>.yml` when `--output` is omitted. The inference contract is limited to repository-local supported workflow source paths and does not apply to `render-batch`. Update CLI help text, README, SPEC, and tests, and fail explicitly for unsupported or ambiguous inputs.
- Definition of Done: `ghawb render --input workflows/ci.ts` can emit `.github/workflows/ci.yml` without an explicit `--output`, under a documented and tested inference contract. Documentation and code review completed.
- Acceptance Criteria: Omitting `--output` on `render` writes to the inferred `.github/workflows/<basename>.yml` path for supported repository-local inputs. The inference rule and error behavior are documented precisely. Existing explicit `--output` usage and `render-batch` semantics remain unchanged.
- Story Points: 2
- Status: ready
- Completed At: N/A
- Notes/Links: User-requested CLI ergonomics improvement after Sprint 18. Scope tightened during Sprint 20 refinement to avoid implicit `render-batch` behavior drift.

### Item 69: Remaining niche trigger support

- Why: The project still advertises a small set of unsupported trigger types in `README.md` and `docs/SYNTAX_COVERAGE.md`, leaving a visible syntax-coverage gap after the major trigger work is otherwise complete. Closing the remaining niche trigger set would reduce the "mostly complete except for edge cases" status of the SDK.
- Prerequisites: None. Builds on the existing trigger model/builder/validation/renderer/conformance pattern.
- Implementation Plan: Implement the remaining explicitly documented unsupported workflow triggers, starting with `branch_protection_rule` and `deployment_protection_rule`, and either include any GitHub App event work that cleanly fits in the same slice or re-scope that remaining gap into a precise follow-up item with updated docs. Update builder APIs, validation, rendering, conformance fixtures, `docs/SPEC.md`, `docs/SYNTAX_COVERAGE.md`, and README support claims.
- Definition of Done: The currently documented unsupported niche triggers are either implemented or explicitly re-scoped into a narrower follow-up backlog item with updated docs. Tests, cross-runtime conformance coverage, documentation, and code review completed.
- Acceptance Criteria: `branch_protection_rule` and `deployment_protection_rule` are supported through the SDK and renderer with documented behavior. Any remaining GitHub App event gap is documented precisely rather than described vaguely. The corresponding rows are removed or narrowed in `docs/SYNTAX_COVERAGE.md` and README.
- Story Points: 3
- Status: ready
- Completed At: N/A
- Notes/Links: Intake from README "Not Yet Supported" and `docs/SYNTAX_COVERAGE.md` "Not Yet Supported Triggers".

### Item 70a: Composite action definition discovery spike

- Why: Composite actions remain the only feature listed under `docs/SYNTAX_COVERAGE.md` "Not Yet Supported Features", but the right package boundary, AST ownership, and output path are still open. A discovery-first spike is needed so implementation does not bury those decisions inside delivery work.
- Prerequisites: None.
- Implementation Plan: Compare at least an `@ghawb/sdk`-hosted path and a separate-package path, define the minimum action-level AST surface needed for a first slice, decide whether output should stay on an existing CLI path or move through a dedicated action-emitter flow, and record the smallest implementation-safe next slice plus explicit unsupported scope.
- Definition of Done: A documented recommendation exists for the preferred package and emission path, rejected alternatives are summarized briefly, the supported and unsupported first-slice scope is explicit, and the follow-up implementation item is backlog-ready without major open design questions.
- Acceptance Criteria: The spike evaluates at least two package-boundary options, clarifies AST ownership, states the chosen output path, defines the initial supported slice for composite actions, and leaves `Item 70b` with no major unresolved design ambiguity.
- Story Points: 2
- Status: ready
- Completed At: N/A
- Notes/Links: Split from original Item 70 during Sprint 20 refinement to follow the repository's discovery-first pattern for architecture-adjacent work.

### Item 70b: Composite action definition initial implementation slice

- Why: Once the discovery spike closes the boundary decisions, the repository still needs an actual first supported composite-action authoring slice to reduce the documented feature gap.
- Prerequisites: Item 70a.
- Implementation Plan: Implement only the minimal composite-action authoring slice selected by `Item 70a`, including the chosen output path, validation behavior, tests, and documentation updates needed to prove the first supported `action.yml`-oriented contract.
- Definition of Done: The first composite-action definition slice chosen by `Item 70a` exists in code with tests, documentation, and code review, and it does not leave major design questions unresolved inside the delivered slice.
- Acceptance Criteria: Users can author the explicitly supported first composite-action slice through the API and render the expected output artifact on the chosen path. Unsupported composite-action features remain documented precisely instead of being implied.
- Story Points: 3
- Status: ready
- Completed At: N/A
- Notes/Links: Follow-up delivery item created by splitting original Item 70 into discovery plus implementation phases during Sprint 20 planning.

### Item 71: Expand typed action wrappers with about 10 popular actions

- Why: `@ghawb/typed-actions` now proves the opt-in wrapper boundary, but the current wrapper set only covers four first-party actions. Expanding the package with a broader set of high-frequency actions would increase practical value for users who want typed `with` inputs on the actions they use most often.
- Prerequisites: None. Builds on the newly separated `@ghawb/typed-actions` package and the `TypedActionStep` core that remains in `@ghawb/sdk`.
- Implementation Plan: Select roughly 10 popular GitHub Actions to add as manual-first typed wrappers in `@ghawb/typed-actions`. Favor widely used first-party or ecosystem-standard actions with stable input surfaces and clear version pins. Document the selection rationale, implement typed input mappings with string serialization, add tests, and update README/API/SPEC docs to show the expanded wrapper catalog.
- Definition of Done: `@ghawb/typed-actions` ships about 10 additional popular action wrappers beyond the current four, with pinned refs, typed input surfaces, tests, docs, and code review completed.
- Acceptance Criteria: The package exposes typed wrappers for approximately 10 newly selected popular actions. Wrapper names and input field naming follow the repository's TypeScript style (for example `exampleUrl`, `runId`, `nodeCi`). The selected actions and version pins are documented, and `.uses(...)` examples demonstrate the expanded value of the opt-in package.
- Story Points: 5
- Status: ready
- Completed At: N/A
- Notes/Links: User-requested backlog intake after the `@ghawb/typed-actions` package split. Selection should prefer popularity plus API stability over exhaustiveness.

### Item 72: Add typed action examples to README and COOKBOOK

- Why: The repository now documents that `@ghawb/typed-actions` exists, but README and COOKBOOK do not yet show concrete examples of using typed wrappers. That leaves the feature discoverable only through API-reference reading instead of through the primary onboarding docs.
- Prerequisites: None. Builds on the existing `@ghawb/typed-actions` package and should remain aligned with whatever wrapper set is currently shipped.
- Implementation Plan: Add at least one concise typed-wrapper example to `README.md` and at least one practical recipe to `docs/COOKBOOK.md`. Show imports from `@ghawb/typed-actions`, demonstrate `.uses(...)` with typed wrappers, and explain when to prefer wrappers versus raw action refs or `nodeCi()`.
- Definition of Done: README and COOKBOOK both include accurate typed-action examples that run against the current shipped API, with tests or example validation updated as needed and code review completed.
- Acceptance Criteria: README contains a typed-action usage example, COOKBOOK contains a typed-action recipe, imports use `@ghawb/typed-actions`, and the examples reflect the current TypeScript naming style and current wrapper package boundary.
- Story Points: 1
- Status: ready
- Completed At: N/A
- Notes/Links: User-requested backlog intake after confirming that README and COOKBOOK currently lack concrete typed-action examples.
