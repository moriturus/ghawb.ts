# Sprint Backlog: Sprint 21

This document records the selected sprint backlog and planning decisions for Sprint 21.

## Sprint Summary

Capacity: 20 story points.

Selected implementation units for Sprint 21: 9/20 story points.

Status: complete — 3/3 items completed (9/9 SP).

## Planning Notes

- Capacity decision: Sprint 21 keeps the standing 20 SP capacity and commits 9 SP, leaving 11 SP residual. The underfill is intentional: the team preserves hardening-first delivery order and does not pull unresolved package-boundary work into the sprint just to consume capacity.
- Selection decision: Sprint 21 commits Items 74, 76, and 73 for a total of 9 SP. The sequence keeps the hardening-oriented workflow modernization and Deno compatibility work ahead of the broader onboarding documentation expansion.
- Ordering decision: Sprint 21 preserves strict backlog execution order: `Item 74` -> `Item 76` -> `Item 73`. This follows the current product backlog order and the repository rule that the sprint backlog executes from the top selected item downward.
- Deferral decision: Original Item 75 is split into `Item 75a` (discovery) and `Item 75b` (implementation), and both remain in the product backlog. The Product Owner chose not to commit either split item into Sprint 21 because package-boundary rework around `nodeCi()` would overlap with Item 74 self-hosting modernization and would violate the recent hardening-first planning direction.
- Item 74 scope decision: The workflow-source rewrite must stay inside already shipped authoring surfaces. It should treat committed workflow sources and generated `.github/workflows/*.yml` files as release-surface artifacts and must not force new APIs merely to simplify the rewrite.
- Item 76 scope decision: Deno coverage expansion remains compatibility-oriented rather than a repository-wide test-strategy change. Bun-run Vitest stays the primary integrated quality authority, and Node remains the secondary runtime confirmation path.
- Item 73 sequencing decision: Documentation expansion is committed only after the two hardening-oriented items. The item should absorb the results of Sprint 21 implementation work where helpful, but it must not reopen package-boundary or architecture decisions deferred to the split Item 75 follow-up.
- Collaboration decision: Sprint execution remains single-item-at-a-time in committed order. Sub-agent or multi-agent collaboration is preferred only inside the active item when it improves separation of concerns. Primary implementation responsibility: Mio Kanda (Items 74 and 76) and Yui Morita (Item 73). Verification and review focus: Haru Nishimura. Product-surface acceptance and scope judgment: Aoi Sakamoto. Planned review ownership: Haru Nishimura reviews Items 74 and 76 for correctness and coverage; Aoi Sakamoto reviews Item 73 for onboarding clarity and product-surface fit.
- Verification decision: Sprint 21 continues to use targeted `bun test` commands for item proof, `bun run check` as the primary integrated quality gate, Node-run Vitest as the secondary runtime check, and `bun run verify:workflows` where workflow-definition synchronization is part of the item contract. Deno remains compatibility-oriented coverage rather than the primary sprint test runner.
- Sprint branch: `sprint-21` from `main`, pushed to `origin` before execution starts.

## Committed Items

### Item 74: Rewrite committed `workflows/` sources to use the latest authoring features

- Why: The repository's committed `workflows/*.ts` files are the project's most visible self-hosting examples, but they still reflect older authoring patterns in places. Rewriting them to use the latest stable authoring features would both reduce maintenance friction and turn the repository itself into a stronger proof point for `@ghawb/sdk`, `@ghawb/typed-actions`, and adjacent ergonomics work.
- Prerequisites: None, but the work should stay aligned with the current supported feature set and should not force new APIs just to make the rewrite possible.
- Implementation Plan: Audit the committed `workflows/*.ts` modules and refactor them to use the current preferred surfaces where that materially improves clarity or maintenance. Candidate upgrades include `job.nodeCi()`, typed action wrappers from `@ghawb/typed-actions`, current runner-label constants, clearer reusable patterns already supported by the SDK, and removal of stale hand-written step sequences where the newer abstractions are now stable. Regenerate `.github/workflows/*.yml`, update any affected docs, and verify that self-hosting guardrails and hosted-CI-facing workflow contracts remain green.
- Definition of Done: The committed workflow source modules under `workflows/` consistently reflect the repository's current recommended authoring style where applicable, generated workflow YAML stays in sync, review is complete, and the rewrite does not introduce behavior drift or reduce explicitness.
- Acceptance Criteria: At least the primary committed CI/publish workflow sources are rewritten to use the latest stable authoring features already shipped by the repository. The generated `.github/workflows/*.yml` files are updated from those sources. `bun run verify:workflows`, `bun run check`, and the relevant Node compatibility verification continue to pass after the rewrite.
- Story Points: 3
- Status: done
- Completed At: 2026-04-05T03:31:49Z
- Notes/Links: Moved from the active product backlog into Sprint 21 after the Sprint 20 hardening-first reprioritization. Planned primary implementer: Mio Kanda. Planned reviewer: Haru Nishimura. Delivered by rewriting committed workflow sources to use `RunnerLabel` constants plus typed wrappers from `@ghawb/typed-actions` where stable wrappers already existed, while keeping raw `uses()` calls for unsupported or niche actions such as Bun setup, Deno setup, and Changesets release automation. Verification: `bun run generate:workflows`, `bun run verify:workflows`, `bun run check`, and `bun run test:vitest:node` passed. Review evidence and Product Owner acceptance were recorded on PR #88.

### Item 76: Strengthen Deno compatibility coverage with higher-value test slices

- Why: The repository claims Deno 2.x compatibility and already runs a small Deno test suite, but the documented testing split still treats Deno primarily as smoke-level coverage. That leaves room for runtime-specific regressions in module resolution, package entry points, and representative public API contracts to slip through until late verification.
- Prerequisites: None. The work should preserve Bun-run Vitest as the primary unit/integration authority unless a separate specification or planning decision explicitly changes the repository-wide testing strategy.
- Implementation Plan: Audit the current `tests/deno` coverage and add the highest-value Deno-only or Deno-sensitive verification slices beyond the current smoke baseline. Favor explicit compatibility checks for exported package entry points, representative public API authoring flows, and failure modes that have historically differed under Deno's stricter runtime or type-checking behavior. Update quality commands and documentation only where the strengthened Deno contract materially changes contributor expectations.
- Definition of Done: The repository has materially stronger automated Deno compatibility evidence than the current smoke-oriented baseline, the added tests target concrete cross-runtime risk areas, related documentation is updated where necessary, and the completed change has code review finished by a non-implementing persona.
- Acceptance Criteria: `tests/deno` covers more than a minimal smoke path and includes at least one additional high-value contract slice beyond the existing render-conformance fixtures; the added Deno tests are reproducible through the repository's documented commands; any changed expectations about Deno's verification role are documented consistently across backlog and contributor-facing docs; and the expanded suite does not weaken existing Bun/Node verification.
- Story Points: 3
- Status: done
- Completed At: 2026-04-05T03:38:00Z
- Notes/Links: Moved from the active product backlog into Sprint 21 as the second hardening-oriented item after workflow-definition modernization. Planned primary implementer: Mio Kanda. Planned reviewer: Haru Nishimura. Delivered by expanding `tests/deno` with representative public-entrypoint compatibility coverage for `@ghawb/sdk`, `@ghawb/composite-actions`, and `@ghawb/typed-actions`, plus the repository-local Deno import-map entry needed to exercise `@ghawb/typed-actions` under Deno. Documentation now describes Deno as compatibility-oriented coverage rather than smoke-only checks. Verification: `bun run test:deno`, `bun run check`, and `bun run test:vitest:node` passed. Review evidence and Product Owner acceptance were recorded on PR #89.

### Item 73: Enrich README and COOKBOOK for stronger onboarding and recipe coverage

- Why: The current README and COOKBOOK cover the baseline surfaces, but they are still relatively thin as onboarding material. Stronger examples, decision guidance, and recipe coverage would reduce time-to-first-success and make the project easier to evaluate without reading the full specification.
- Prerequisites: None. May absorb the output of Item 72 if the Product Owner later chooses to sequence documentation work incrementally.
- Implementation Plan: Expand `README.md` and `docs/COOKBOOK.md` with better onboarding structure, more representative workflow examples, clearer package-boundary guidance, and stronger cross-links into API/SPEC material. Favor practical scenarios over exhaustive repetition, and keep examples aligned with the current public API and supported feature set.
- Definition of Done: README and COOKBOOK are materially more useful as user-facing onboarding docs, with improved examples, clearer guidance, updated links, and code review completed.
- Acceptance Criteria: README better explains when to use `@ghawb/sdk`, `@ghawb/typed-actions`, `@ghawb/cli`, and `@ghawb/reusable-workflow-import`; COOKBOOK gains additional high-value recipes beyond the current baseline set; examples are internally consistent with the current implementation and documentation set.
- Story Points: 3
- Status: done
- Completed At: 2026-04-05T03:47:31Z
- Notes/Links: Moved from the active product backlog into Sprint 21 only after the two hardening-oriented items. Planned primary implementer: Yui Morita. Planned reviewer: Aoi Sakamoto. Delivered by adding explicit package-selection and adoption guidance to `README.md`, expanding `docs/COOKBOOK.md` with reusable-workflow and `@ghawb/reusable-workflow-import` recipes, and correcting onboarding/API drift in `docs/API_REFERENCE.md` so the documented methods and examples match the shipped SDK. Verification: `bun run verify:pre-push` passed. Review evidence and Product Owner acceptance were recorded on PR #90.

## Sprint Review Outcome

- Review note: [docs/sprint_reviews/sp21.md](../sprint_reviews/sp21.md)
- Delivered scope decision: Sprint 21 delivered all committed backlog scope (3/3 items, 9/9 SP) with no carry-over.
- Product priority decision: No active-backlog reprioritization was needed; the next backlog order remains Item 75a followed by Item 75b.

## Sprint Retrospective Outcome

- Retrospective note: [docs/sprint_retrospectives/sp21.md](../sprint_retrospectives/sp21.md)
- Team improvement decision: Add an item-closeout doc-surface sync check so onboarding-doc changes also trigger API reference and contributor-doc consistency review before merge.
- Product improvement decision: Keep Item 75a followed by Item 75b as the active backlog order, and append a separate backlog follow-up for `@ghawb/reusable-workflow-import` Deno compatibility hardening.
