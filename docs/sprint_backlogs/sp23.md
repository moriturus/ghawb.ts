# Sprint Backlog: Sprint 23

This document records the selected sprint backlog and planning decisions for Sprint 23.

## Sprint Summary

Capacity: 20 story points.

Selected implementation units for Sprint 23: 5/20 story points.

Status: done — Both committed items are complete and the sprint closeout PR into `main` has merged.

## Planning Notes

- Capacity decision: Sprint 23 keeps the standing 20 SP capacity and commits 5 SP, leaving 15 SP residual. The underfill is intentional because only Items 77 and 78 remain in the active product backlog, and the repository does not permit pulling later work out of order just to consume more capacity.
- Selection decision: Sprint 23 commits Items 77 and 78 for a total of 5 SP. No active product-backlog items remain after this sprint selection.
- Ordering decision: Sprint 23 preserves strict backlog execution order: `Item 77` -> `Item 78`.
- Dependency decision: `Item 78` depends on completed `Item 75b`, which is already done. Sprint execution still proceeds one active backlog item at a time, so `Item 78` does not begin until `Item 77` is review-complete or explicitly stopped by Product Owner direction.
- Item 77 scope decision: Treat the current `@ghawb/yaml-import` behavior as shipped, reproduce the Deno permission-sensitive parser failure through representative package-entrypoint usage, and then either implement the smallest safe hardening slice or document an intentional Deno compatibility boundary if that is the correct product outcome.
- Item 78 scope decision: Add a generic helper-application hook on `JobBuilder` without reintroducing helper-specific SDK methods or prototype mutation. Keep `nodeCi()` in `@ghawb/job-helpers` and adapt the supported authoring pattern toward builder-style helper application such as `job.apply(nodeCi({ nodeVersion: "22" }))`.
- Collaboration decision: Sprint execution remains single-item-at-a-time in committed order. Primary implementation responsibility: Haru Nishimura (`Item 77`) and Mio Kanda (`Item 78`). Planned review ownership: Mio Kanda reviews `Item 77` for package-boundary and API-surface safety; Haru Nishimura reviews `Item 78` for behavior and coverage correctness. Product-surface acceptance and scope judgment: Aoi Sakamoto. Tooling and documentation friction support: Yui Morita.
- Verification decision: Sprint 23 keeps `bun run check` as the primary integrated quality gate, Node-run Vitest as the secondary runtime confirmation path, and targeted Deno tests as compatibility proof only where the active item specifically touches Deno. `bun run verify:workflows` remains required if self-hosted workflow-definition sources change during execution.
- Sprint branch: `sprint-23` from `main`; pushing the branch to `origin` remains required before execution starts.

## Committed Items

### Item 77: Harden `@ghawb/yaml-import` Deno compatibility for representative import flows

- Why: Sprint 21 strengthened Deno compatibility evidence for most public entrypoints, but an attempted expansion into `@ghawb/yaml-import` exposed permission-sensitive behavior in the YAML parsing path under Deno. That leaves one public package with less proven Deno compatibility than adjacent surfaces and turns a sprint-discovered issue into explicit product debt.
- Prerequisites: None. The work should treat the current `@ghawb/yaml-import` behavior as shipped, reproduce the Deno-specific failure mode directly, and decide whether the right fix is implementation hardening, dependency isolation, or an explicit compatibility boundary.
- Implementation Plan: Reproduce the Deno permission-sensitive parser behavior triggered during representative reusable-workflow import tests, isolate whether the environment access comes from the package integration or the underlying YAML stack, and implement the smallest safe hardening slice. Add targeted Deno coverage for representative `@ghawb/yaml-import` entrypoint usage and document any compatibility boundary that remains intentional after the fix.
- Definition of Done: The Deno failure mode is reproduced and either fixed or explicitly documented as an intentional boundary, `@ghawb/yaml-import` has representative Deno compatibility coverage aligned with the chosen boundary, affected docs are updated, and the change completes with code review.
- Acceptance Criteria: The work identifies the concrete cause of the Deno permission-sensitive parser behavior, states whether `@ghawb/yaml-import` remains part of the repository's supported Deno compatibility story, adds at least one targeted Deno verification slice for the package's public entrypoint, and updates contributor or product docs if the supported boundary changes.
- Story Points: 2
- Status: done
- Completed At: 2026-04-05T12:30:00Z
- Notes/Links: Moved from the active product backlog into Sprint 23 after Sprint 22 finished the `nodeCi()` package-boundary migration and left this hardening item as the next top priority. Planned primary implementer: Haru Nishimura. Planned reviewer: Mio Kanda. Delivered by reproducing the Deno permission failure to `yaml@2.8.3` debug env probes, hardening `@ghawb/yaml-import` so representative imports do not require `--allow-env`, adding targeted Deno entrypoint coverage, and aligning `docs/SPEC.md` / `docs/LEARN.md`. Review evidence and Product Owner acceptance were recorded on PR #96. See [Sprint 21 Retrospective](../sprint_retrospectives/sp21.md), [Sprint 21 Review](../sprint_reviews/sp21.md), and [Sprint 22 Backlog](./sp22.md).

### Item 78: Add a generic `JobBuilder` helper-application hook for opt-in job helpers

- Why: Sprint 22 correctly moved `nodeCi()` out of `@ghawb/sdk` into `@ghawb/job-helpers`, but the resulting `nodeCi(job, options)` call shape is less ergonomic than the prior builder-style method call. The product still wants an explicit, package-safe way to apply opt-in job helpers without falling back to prototype mutation or reintroducing helper-specific methods on the core SDK surface.
- Prerequisites: Item 75b. The work should treat `@ghawb/job-helpers` as the approved package boundary and preserve the decision not to use TypeScript prototype extension as the integration mechanism.
- Implementation Plan: Add the smallest safe generic hook on `JobBuilder` for applying externally defined helper functions, then adapt `@ghawb/job-helpers` so `nodeCi()` can be consumed through that hook in builder style (`j.apply(nodeCi({ nodeVersion: "22" }))`). Keep the API additive and explicit, document the chosen naming and usage pattern, and avoid reintroducing helper-specific behavior into `@ghawb/sdk`.
- Definition of Done: `JobBuilder` exposes a reviewed generic helper-application hook, `nodeCi()` uses that hook from `@ghawb/job-helpers`, the resulting authoring pattern is documented with migration guidance, verification covers the new builder-style usage, and the change completes with code review.
- Acceptance Criteria: The delivered API lets callers apply opt-in job helpers through a generic `JobBuilder` hook instead of passing the builder as a positional argument, does not rely on prototype mutation or helper-specific SDK methods, keeps `nodeCi()` in `@ghawb/job-helpers`, updates `docs/SPEC.md` and affected public docs, and proves through tests that the supported authoring contract remains intact.
- Story Points: 3
- Status: done
- Completed At: 2026-04-05T12:37:00Z
- Notes/Links: Moved from the active product backlog into Sprint 23 as the next item after the Item 77 hardening slice. Planned primary implementer: Mio Kanda. Planned reviewer: Haru Nishimura. Delivered by adding a generic `JobBuilder.apply(helper)` hook, adapting `nodeCi()` toward the preferred builder-style `job.apply(nodeCi({ nodeVersion: "22" }))` contract while preserving `nodeCi(job, options)` for migration, and aligning README / Cookbook / API reference / SPEC docs with the new pattern. Review evidence and Product Owner acceptance were recorded on PR #97. See [ADR 0002](../adrs/0002-scope-job-recipes-to-a-narrow-node-ci-helper.md), [ADR 0006](../adrs/0006-migrate-node-ci-helper-to-opt-in-package.md), [Sprint 22 Backlog](./sp22.md), and [API Reference](../API_REFERENCE.md).
